---
description: Humanize prose or markdown — remove AI writing patterns
---

Load the humanizer skill (call the skill tool with name "humanizer") and follow its process. Apply it to $ARGUMENTS. If no input is given, humanize the most recent text in the conversation.
